services:
  - type: web
    name: sistema-compra
    runtime: java
    buildCommand: ./gradlew bootJar
    startCommand: java -jar build/libs/SistemaCompraRepository-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17